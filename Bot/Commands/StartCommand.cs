namespace Ch_Chatbot.Bot.Commands;

/// <summary>
/// Placeholder for /start command-specific logic.
/// As the bot grows, each command may require its own class with complex validation or data fetching.
/// For now, the actual response text lives in CommandService.
/// </summary>
public sealed class StartCommand
{
    // TODO: implement dedicated logic when /start requires more than a simple reply
    //       (e.g. onboarding flow, language selection, character intro).
}
